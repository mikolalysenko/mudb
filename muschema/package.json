{
  "name": "muschema",
  "version": "0.0.1",
  "description": "Schemas for mudb",
  "main": "index.js",
  "scripts": {
    "test": "ts-node src/test/index.js",
    "do-publish": "tsc && cp package.json build/ && cp README.md build/ && npm publish build/",
    "do-link": "cp package.json src/ && cd src && npm link ."
  },
  "keywords": [
    "mudb",
    "schema",
    "diff",
    "patch",
    "binary"
  ],
  "author": "Mikola Lysenko",
  "license": "MIT",
  "devDependencies": {
    "tape": "^4.8.0",
    "ts-node": "^3.3.0",
    "typescript": "^2.5.2"
  }
}
